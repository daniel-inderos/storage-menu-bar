# Implementation Plans

Generated by the improve skill on 2026-07-06 against commit `cd2e253` (v1.3.0).
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, update your
status row when done, and **append the required Verification report** (with
before/after reproduction steps and real command output) to the bottom of your
plan file — a plan without its report is not DONE.

Verification baseline at planning time: `swift test` → 24 tests, 0 failures.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Make refresh cost proportional to what is visible | P1 | M | — | DONE (approved 2026-07-06; branch `worktree-agent-a021bd63697490c7b`, commits `95ce647`+`e7c595b`+`bd9d6d4`; merged to main dc2523a) |
| 002 | Put the battery estimation helpers under test | P2 | S | — | DONE (approved 2026-07-06; branch `worktree-agent-a76bed21817b3cbaa`, commit `a6988fc`; merged to main dc2523a) |
| 003 | Put ReclaimScanner's cache and result mapping under test | P3 | M | — | DONE (approved 2026-07-06; branch `worktree-agent-af270119c60e8d56e`, commits `fbcd88e`+`f94115c`; merged to main dc2523a) |
| 004 | Replace ssh-keyscan TOFU with verified GitHub host keys | P2 | S | — | DONE (approved 2026-07-06 after 1 revision; branch `advisor/005-notarization-prep`, commits `d715e9a`+`ba63c13`; live e2e proof at next `v*` tag) |
| 005 | Developer ID signing + notarization pipeline (dormant) | P1 | M | 004 (landed on same branch) | DONE-DORMANT (approved 2026-07-06; commits `b57a5ce`+`2b530ab`; activates when the six signing secrets are configured — checklist in plan) |

Status values: TODO | IN PROGRESS | DONE | DONE-DORMANT (005 only: built, awaiting credentials) | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 004 and 005 both edit `.github/workflows/release.yml`. Land 004 first on its
  own branch; 005's drift check explicitly tolerates 004's known_hosts change.
- 001, 002, 003 are mutually independent (no file overlap) and can run in
  parallel worktrees. 002 should land before anyone modifies battery logic;
  nothing currently planned does.
- 005's live end-to-end verification is blocked on the maintainer enrolling in
  the Apple Developer Program and configuring six repository secrets (named in
  the plan). Deadline pressure: Homebrew removes `--no-quarantine` ~Sept 2026.

## Findings considered and rejected

(So nobody re-audits these.)

- `Prefs.migrateLegacyDefaults` untested: one-shot migration code already
  slated for removal once legacy users upgrade; the injection refactor to test
  it costs more than the code's remaining lifetime justifies.
- `mach_host_self()` send-right reuse in `SystemStats.memory()`/`cpuUsage()`:
  theoretical Mach port uref accumulation; negligible in practice, not worth
  caching the port.
- `UpdateChecker.isVersion` treats prerelease tags (e.g. `v1.4.0-beta`) as
  newer: the repo only tags stable releases; revisit only if beta tags ever ship.
- AppDelegate size (628 lines): cohesive for an app this small; presentation
  logic already extracted to `StatusPresentation`. A further split would be
  churn, not improvement.

## Observations from execution (2026-07-06)

- One transient `swift test` failure (1 of 7 runs, 25 tests) was observed during
  plan 001's review on a machine under heavy parallel build load; 5 consecutive
  clean runs followed. The deterministic tests can't be the cause — suspect a
  `SystemStatsLiveTests` assertion (they check live system state, e.g.
  `memory.used < memory.total`, which can wobble under memory pressure). If it
  recurs in CI, that's a small follow-up finding: loosen or retry the live tests.

## Housekeeping (completed 2026-07-06)

- Merged branches deleted (local + `origin/1sec-refresh`); executor worktrees removed.
- `prompt-exports/` and `.claude/` added to `.gitignore`.
